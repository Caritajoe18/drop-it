{
  "name": "drops",
  "version": "1.0.0",
  "private": true,
  "description": "A micro task platform with USDC payments on the Hedera network",
  "scripts": {
    "install:all": "cd apps/backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd apps/backend && npm run dev",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "build:backend": "cd apps/backend && npm run build",
    "build:frontend": "cd apps/frontend && npm run build",
    "lint:backend": "cd apps/backend && npm run lint",
    "lint:frontend": "cd apps/frontend && npm run lint",
    "test:backend": "cd apps/backend && npm run test",
    "db:migrate": "cd apps/backend && npm run db:migrate",
    "db:seed": "cd apps/backend && npm run db:seed",
    "db:migrate:undo": "cd apps/backend && npm run db:migrate:undo"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
