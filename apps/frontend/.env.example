# Backend base URL (no trailing slash).
# Leave empty in local dev — the Vite proxy handles /drop/* automatically.
# On Vercel (or any production host) set this to your deployed backend URL.
VITE_API_URL=https://your-backend-url.com
